<h1>Burgers!</h1>

<h2>Burgers that you can eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>{{burger_name}}<button data-id = {{id}}>Eat It</button></li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you ate!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>{{burger_name}}</li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="adddingBurger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
